---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---

<div class="rounded-2xl border border-line bg-panel px-5 py-4">
  <p class="display text-3xl text-accent">{value}</p>
  <p class="mt-1 text-base leading-snug text-muted">{label}</p>
</div>
